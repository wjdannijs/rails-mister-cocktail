<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>
  <%= f.association :ingredient %>
  <%= f.input :description, paceholder: "Description for the dose"  %>
  <%= f.submit "add a dose", class: "btn btn-primary" %>
<% end %>
